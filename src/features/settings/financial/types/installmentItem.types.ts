export type InstallmentPolicyItem = {
  id: number;

  installmentNumber: number;

  title: string;

  percentage: number;

  dueMonth: number;

  dueDay: number;

  createdAt: string;

  updatedAt: string;
};