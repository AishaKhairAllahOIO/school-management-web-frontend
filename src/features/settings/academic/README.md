# Academic Settings Workspace Feature

Place this folder under:

`src/features/settings/academic`

The feature is currently connected to a Mock API with full CRUD behavior.
Later, replace the functions in `api/academicSettings.api.ts` with real HTTP calls and keep the UI/hooks unchanged.
